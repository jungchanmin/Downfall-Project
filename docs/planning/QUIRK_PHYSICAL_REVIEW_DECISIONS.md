# Physical Quirk Review Decisions

- `#Trait_Phantom_Scabies`는 육체 기벽에서 제거하고 정신 기벽 DB의 환촉성 감각 이상으로 재분류한다.
- `내이 파열`은 완전 파열이 아닌 `내이 손상 (만성 이명)`으로 명칭과 설정을 조정하고 치료 등급 `hard`를 유지한다.
- `만성 두개 박동`은 `#Stat_Intelligence -1`과 조사·연구 행동 판정 modifier -1로 조정한다.
- 실제 밸런스 테스트에서 문제가 발생하면 수치만 재조정하고 분류 방향은 유지한다.
